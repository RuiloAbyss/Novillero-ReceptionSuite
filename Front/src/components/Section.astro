---
const {
  title = "",
  className = "",
  sectionId = "",
}: {
  title?: string;
  className?: string;
  sectionId?: string;
} = Astro.props;

---

<section
  id={sectionId}
  class={`${className} my-10`} 
>
  <h3 class="mb-5 text-center">{title}</h3>
  <slot />
</section>
